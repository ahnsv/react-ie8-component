const React = require('react');

export default class CalendarView extends React.Components {
    constructor(prop) {
        super(prop)
    }
    render() {
        return (
            <div>
                CalendarView
            </div>
        )
    }
}